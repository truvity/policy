kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# One node. A multi-node cluster would test scheduling, which nothing in a
# chart's contract depends on, and would cost minutes on every run.
nodes:
  - role: control-plane
