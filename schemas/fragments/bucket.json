{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/truvity/policy/schemas/fragments/bucket.json",
  "title": "bucket",
  "description": "An object store addressed by the S3 API. The vendor is not part of the configuration: an endpoint, a region and a path-style flag are enough to reach a cloud service, an in-cluster store or a test double, and a service that hard-codes one of them cannot be tested without it.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "The bucket. It exists already: a service does not create its own store."
    },
    "region": {
      "type": "string",
      "minLength": 1,
      "description": "The region the bucket is in."
    },
    "endpoint": {
      "type": "string",
      "format": "uri",
      "description": "Override the API endpoint. Unset means the SDK's own resolution for the region."
    },
    "pathStyle": {
      "type": "boolean",
      "default": false,
      "description": "Address the bucket as a path rather than as a host. Required by most non-cloud implementations."
    },
    "credentialsEnv": {
      "type": "object",
      "additionalProperties": false,
      "description": "The NAMES of the environment variables holding the credentials, never the values. Unset means the SDK's ambient credentials, which is what a workload identity provides.",
      "properties": {
        "accessKeyID": { "type": "string", "minLength": 1 },
        "secretAccessKey": { "type": "string", "minLength": 1 }
      },
      "required": ["accessKeyID", "secretAccessKey"]
    }
  }
}
