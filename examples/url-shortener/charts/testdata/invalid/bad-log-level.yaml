# A level the binary does not accept. The chart refuses it rather than
# rendering a file that would start the service on a default nobody chose.
log:
  level: chatty
database:
  host: pg-rw
  owner:
    passwordSecret: pg-owner
  app:
    passwordSecret: pg-app
events:
  url: nats://nats:4222
