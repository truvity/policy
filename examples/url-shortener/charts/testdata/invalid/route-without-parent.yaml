# A route with no parent attaches to nothing, renders as healthy with zero
# attached routes, and answers 404 with no explanation.
image:
  tag: dev
route:
  enabled: true
database:
  host: pg-rw
  owner:
    passwordSecret: pg-owner
  app:
    passwordSecret: pg-app
events:
  url: nats://nats:4222
