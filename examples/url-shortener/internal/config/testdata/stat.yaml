probes:
  address: ":7070"
log:
  level: info
database:
  url: postgres://stat@url-shortener-pg-rw:5432/url_shortener?sslmode=require
  passwordEnv: DATABASE_PASSWORD
events:
  nats:
    url: nats://nats.nats.svc:4222
  consumer:
    stream: URL_SHORTENER_STAT
    durable: stat
    subject: url-shortener.redirect
